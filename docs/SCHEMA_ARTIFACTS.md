# Published Schemas

Stable machine-to-machine schemas live under `iocparser/schemas/`.

Current artifacts:

- `pipeline-job-result-1.0.json`
- `batch-report-1.0.json`
- `distributed-job-1.0.json`

Release automation uploads them as CI artifacts and release assets so downstream consumers can pin exact versions.
